CREATE INDEX song_index ON song (song_name);

CREATE INDEX nick_index ON users (nick);

CREATE INDEX album_name_index ON album (album_name);

CREATE INDEX artist_surname_index ON artist (surname);

CREATE INDEX band_name_index ON band (band_name);